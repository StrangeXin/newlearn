// ===========================================================================
// src/lib/learn-limits.ts —— 学习闭环的字数 / 节奏常量（PRD §6 / §15.6）。
// 纯常量、零依赖：客户端表单与服务端校验共用同一来源，避免两边各写一份悄悄漂移。
// （@/lib/learn 会再导出这些常量给其既有消费者。）
// ===========================================================================

/** 笔记字数下限（PRD §6 步骤 2）。 */
export const NOTE_MIN = 100;
/** 笔记字数上限（PRD §15.6 由 5000 收紧到 2000）。 */
export const NOTE_MAX = 2000;
/** 每条追问回答字数上限（PRD §6 步骤 5）。 */
export const ANSWER_MAX = 1000;
/** 结果页追加提问字数上限（PRD §15.6）。 */
export const QUESTION_MAX = 1000;
/** 每天最多新完成多少个关键词；重刷已完成的词不计入（PRD §6.3）。 */
export const DAILY_COMPLETION_LIMIT = 10;
/** 写笔记 / 答追问草稿自动保存的防抖间隔（毫秒）。 */
export const AUTOSAVE_MS = 1200;
/** 单个草稿字段落库的硬上限（防滥用，宽于 NOTE_MAX 以容下编辑中文本）。 */
export const DRAFT_MAX = 12000;
